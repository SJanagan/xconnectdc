export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with XCONNECT Datacenters for inquiries about data center services, colocation, cloud solutions, and connectivity.',
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
