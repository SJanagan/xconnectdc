export const metadata = {
  title: 'Services',
  description: 'Explore XCONNECT Datacenters comprehensive services including Colocation, Cloud, OPGW Connectivity, Network Interconnection, Disaster Recovery, and Security & Compliance.',
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
